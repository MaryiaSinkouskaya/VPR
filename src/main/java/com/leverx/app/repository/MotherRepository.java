package com.leverx.app.repository;

import com.leverx.app.entity.Mother;
import org.springframework.data.jpa.repository.JpaRepository;

public interface MotherRepository extends JpaRepository<Mother, Long> {
}