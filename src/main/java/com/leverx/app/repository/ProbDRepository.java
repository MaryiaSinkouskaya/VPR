package com.leverx.app.repository;

import com.leverx.app.entity.ProbD;
import org.springframework.data.jpa.repository.JpaRepository;

public interface ProbDRepository extends JpaRepository<ProbD, Long> {
}